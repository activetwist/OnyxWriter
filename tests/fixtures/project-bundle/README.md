# Project Bundle Fixture

This fixture represents a source project that contains an OKF bundle under `docs/okf`.

Onyx Writer should treat `docs/okf` as the bundle root. The project root itself is intentionally shaped like a code repository and should trigger project-root safeguards in the desktop app.
